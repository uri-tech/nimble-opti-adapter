---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: nimble-opti-adapterconfigs.config.nimble-opti-adapter.tech-ua.com
spec:
  group: config.nimble-opti-adapter.tech-ua.com
  names:
    kind: nimble-opti-adapterConfig
    listKind: nimble-opti-adapterConfigList
    plural: nimble-opti-adapterconfigs
    singular: nimble-opti-adapterconfig
  scope: Namespaced
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description:
            nimble-opti-adapterConfig is the Schema for the nimble-opti-adapterconfigs
            API
          properties:
            apiVersion:
              description:
                "APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the latest
                internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
              type: string
            kind:
              description:
                "Kind is a string value representing the REST resource this
                object represents. Servers may infer this from the endpoint the client
                submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
              type: string
            metadata:
              type: object
            spec:
              description:
                nimble-opti-adapterConfigSpec defines the desired state of
                nimble-opti-adapterConfig
              properties:
                annotationRemovalDelay:
                  description:
                    'AnnotationRemovalDelay is the delay (in seconds) after
                    removing the "nginx.ingress.kubernetes.io/backend-protocol: HTTPS"
                    annotation before re-adding it'
                  type: integer
                certificateRenewalThreshold:
                  description:
                    CertificateRenewalThreshold is the waiting time (in days)
                    before the certificate expires to trigger renewal
                  type: integer
                renewalCheckInterval:
                  description:
                    RenewalCheckInterval is the interval (in minutes) for
                    checking certificate renewals
                  type: integer
                targetNamespace:
                  description:
                    TargetNamespace is the namespace where the operator should
                    manage certificates
                  type: string
              required:
                - annotationRemovalDelay
                - certificateRenewalThreshold
                - renewalCheckInterval
                - targetNamespace
              type: object
            status:
              description:
                nimble-opti-adapterConfigStatus defines the observed state
                of nimble-opti-adapterConfig
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
